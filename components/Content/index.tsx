import * as React from 'react';
import styled from 'styled-components';

const ContentWrapper = styled.div`
    font-size: 24px;
`;

const Content = () => (
	<ContentWrapper>
		Next + React + TypeScript + StyledComponent = 🧡
	</ContentWrapper>
);

export default Content;
